extends ../../../global/views/layouts/mainCafedra.pug

block content
  - let counter = 0;
  h2 Предмети
  - if (data)
    table
      thead
        tr
          th №п/п 
          th Предмети
      tbody
        each item in data
          tr
            td= ++counter
            td= item.subject
            td
              a(href=`/admin/cafedra/subject/edit/${item._id}`) Редагувати